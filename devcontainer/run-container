#! /usr/bin/sh
podman run \
  --replace \
  --userns=keep-id \
  -v $(pwd):/home/$USER/Workspace:Z \
  -v $XAUTHORITY:$XAUTHORITY:Z \
  -v /tmp/.X11-unix:/tmp/.X11-unix:ro \
  --workdir=/home/$USER/Workspace \
  -e "DISPLAY" \
  -e "WAYLAND_DISPLAY" \
  -e "XAUTHORITY" \
  --security-opt label=type:container_runtime_t \
  --net=host \
  --name devcontainer \
  -h devcontainer \
  -it \
  -t devcontainer

